{
  "name": "novo-portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/api-gateway\" \"npm run dev --workspace=apps/info-service\" \"npm run dev --workspace=apps/blog-service\" \"npm run dev --workspace=apps/web\"",
    "build": "npm run build --workspaces",
    "seed": "npm run seed --workspace=apps/info-service && npm run seed --workspace=apps/blog-service"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

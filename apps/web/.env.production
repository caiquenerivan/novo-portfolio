# Usado automaticamente pelo `vite build` (modo produção).
# Não é segredo — é só a URL pública do gateway. Pode ser sobrescrito
# configurando VITE_API_URL nas variáveis de ambiente do projeto na Vercel.
VITE_API_URL=https://api.nerivan.site/api
